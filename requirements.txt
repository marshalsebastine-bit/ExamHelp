# Core: schemas, catalogue, corpus ingestion
pydantic>=2.7
pyyaml>=6.0
beautifulsoup4>=4.12
lxml>=5.0
pypdf>=5.0

# Retrieval. No FAISS: at this corpus size brute-force cosine over a numpy
# array is exact, instant, and one dependency lighter (repo review 3).
numpy>=1.26
rank-bm25>=0.2.2

# Dense embeddings, needed only for Family C retrieval (week 4).
sentence-transformers>=3.0

# Output surfaces (weeks 4-5)
jinja2>=3.1
python-docx>=1.2.0

# Model gateway: KOMP-01's competency-derivation call (gateway/model_gateway.py)
mistralai>=1.0
groq>=0.11
python-dotenv>=1.0

# Tests
pytest>=8.0
